# 1. Habilita mod_rewrite
RewriteEngine On

# 2. No reescribir solicitudes a archivos o carpetas reales
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# 3. Redirige todo lo demás a index.html
RewriteRule ^ index.html [L]
