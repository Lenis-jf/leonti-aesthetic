# ─────────────────────────────────────────────────────────────
# 0. Iniciar motor y redirigir dominios
# ─────────────────────────────────────────────────────────────
RewriteEngine On

# 0a) Redirigir T O D O leonti.eu → https://www.aesthetic-leonti.de
RewriteCond %{HTTP_HOST} ^(www\.)?leonti\.eu$ [NC]
RewriteRule ^ https://www.aesthetic-leonti.de%{REQUEST_URI} [L,R=301]

# 0b) Forzar HTTPS + www en aesthetic-leonti.de
RewriteCond %{HTTPS} !=on [OR]
RewriteCond %{HTTP_HOST} !^www\.aesthetic\-leonti\.de$ [NC]
RewriteRule ^ https://www.aesthetic-leonti.de%{REQUEST_URI} [L,R=301]

# 0c) HSTS: obliga a usar HTTPS
Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains"

# ─────────────────────────────────────────────────────────────
# 🔧 MODO MANTENIMIENTO: Redirigir todas las peticiones a maintenance.html
# ─────────────────────────────────────────────────────────────
# Evitar bucle infinito para acceder a la propia maintenance.html, assets o favicon
RewriteCond %{REQUEST_URI} !/maintenance\.html$ [NC]
RewriteCond %{REQUEST_URI} !/styles/ [NC]
RewriteCond %{REQUEST_URI} !/assets/ [NC]
RewriteCond %{REQUEST_URI} !/favicon\.ico$ [NC]
RewriteRule ^.*$ /maintenance.html [L]

# ─────────────────────────────────────────────────────────────
# 1. BrowserRouter fallback (SPA)
# ─────────────────────────────────────────────────────────────
RewriteBase /
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]
RewriteRule . /index.html [L]

# ─────────────────────────────────────────────────────────────
# 2. Sitemap XML content type
# ─────────────────────────────────────────────────────────────
<Files "sitemap.xml">
  ForceType application/xml
  Header set Content-Type "application/xml; charset=UTF-8"
</Files>

# ─────────────────────────────────────────────────────────────
# 3. Desactivar listado de directorios
# ─────────────────────────────────────────────────────────────
Options -Indexes

# ─────────────────────────────────────────────────────────────
# 4. Cabeceras de seguridad
# ─────────────────────────────────────────────────────────────
Header set X-Frame-Options "SAMEORIGIN"
Header set X-XSS-Protection "1; mode=block"
Header set X-Content-Type-Options "nosniff"
Header set Referrer-Policy "no-referrer-when-downgrade"
Header set Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' https://maps.googleapis.com https://maps.gstatic.com; connect-src 'self' https://maps.googleapis.com https://maps.gstatic.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com https://maps.gstatic.com; img-src 'self' data: https://maps.gstatic.com https://maps.googleapis.com; font-src 'self' https://fonts.gstatic.com;"

# ─────────────────────────────────────────────────────────────
# 5. Caching
# ─────────────────────────────────────────────────────────────
<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType image/webp "access plus 30 days"
  ExpiresByType image/jpeg "access plus 30 days"
  ExpiresByType image/png  "access plus 30 days"
  ExpiresByType image/svg+xml "access plus 30 days"
  ExpiresByType text/css "access plus 7 days"
  ExpiresByType application/javascript "access plus 7 days"
  ExpiresByType application/json "access plus 7 days"
  ExpiresByType font/woff2 "access plus 30 days"
  ExpiresByType font/woff "access plus 30 days"
</IfModule>

# ─────────────────────────────────────────────────────────────
# 6. Compresión
# ─────────────────────────────────────────────────────────────
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/plain text/html text/xml text/css text/javascript application/javascript application/json
  SetEnvIfNoCase Request_URI \.(?:gif|jpe?g|png|pdf|zip|gz)$ no-gzip
</IfModule>
